ext {
    //年份
    BASE_APK_YEAR = '2019'
    //热修复补丁包所对应的基线版本，发布基线版本时可不用管
    BASE_APK_DIR = 'app-0618-17-50-58'
    //基线版本:base  热修复版本:patch
    EXTEND = 'base'
    //基线版本名称和版本号，发布热修复包时不用更改
    VERSION_NAME = 'jnly_1.0.21'
    VERSION_CODE = 21
    /**
     * *********************************************************************************************
     * *********************************************************************************************
     * *********************************************************************************************
     */

    // 应用程序包名
    APPLICATION_ID = 'com.titan.jnly'
    // 用于编译的SDK版本
    COMPILE_SDK_VERSION = 28
    // 用于Gradle编译项目的工具版本
    BUILD_TOOLS_VERSION = "28.0.2"
    // 最低支持Android版本
    MIN_SDK_VERSION = 19
    // 目标版本
    TARGET_SDK_VERSION = 25
    // 设置是否使用混淆
    MINIFY_ENABLED = false
    // 设置是否在打包时压缩资源
    ZIP_SOURCE = false
    //api引用版本
    APPCOMPAT_VERSION = '25.0.1'
}