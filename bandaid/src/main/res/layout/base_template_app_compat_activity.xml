<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--app:navigationIcon="@drawable/ic_back"-->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/_toolbar"
        style="@style/common_tool_bar_style"
        app:popupTheme="@style/AppTheme.NoActionBar.PopupOverlay"
        app:titleTextColor="@color/white">

        <TextView
            android:id="@+id/_tvToolbarName"
            style="@style/common_tool_bar_style_title" />

        <Button
            android:id="@+id/btnRight"
            style="@style/common_tool_bar_style_button" />

    </androidx.appcompat.widget.Toolbar>


    <!--<FrameLayout
        android:id="@+id/_frameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/_toolbar">

    </FrameLayout>-->

    <com.lib.bandaid.widget.layout.RootStatusView
        android:id="@+id/_frameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/_appBarLayout" />

</RelativeLayout>
